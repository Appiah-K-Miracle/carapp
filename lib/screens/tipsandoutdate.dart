import 'package:flutter/material.dart';
class TipsAndOutdate extends StatelessWidget {
  const TipsAndOutdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Hello"),
      ),
    );
  }
}
